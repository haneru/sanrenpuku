<div class='card mt-5'>
  <div class='card-header'>
    日時選択
  </div>
  <%= form_for @result do |f| %>
    <div class='card-body'>
      <%= f.date_field :collect_date, class: 'form-control' %>
    </div>
    <div class='card-footer'>
      <div class='row'>
        <div class='col-md-6'>
          <%= link_to 'キャンセル', results_path, class: 'btn btn-block' %>
        </div>
        <div class='col-md-6'>
          <%= f.submit '日時を決定', class: 'btn btn-primary btn-block' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
